if ($('#cart').length == 1) { $('#cart').hide(1000); }

$('#cart tr').not('.total_line').remove();

window.location.reload(true);


// $('#cart').html("<%= j render @cart %>");
// $('#cart').hide('blind');